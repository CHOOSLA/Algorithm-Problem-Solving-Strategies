#include <cstdio>
#include <iostream>

using namespace std;

int boxes;
int heating[10000], eating[10000];

int lunch(){
    int result;


    return result;
}

int main(int argc, char const *argv[])
{
    freopen("../../inputs/lunchbox.txt", "r", stdin);
    int cc;
    cin >> cc;
    for(int i = 0; i < cc; ++i)
    {
        cin >> boxes;
        for(int j = 0; j < boxes; ++j)
        {
            cin >> heating[j];
        }
        for(int j = 0; j < boxes; ++j)
        {
            cin >> eating[j];
        }
        
        

    }
    
    return 0;
}
